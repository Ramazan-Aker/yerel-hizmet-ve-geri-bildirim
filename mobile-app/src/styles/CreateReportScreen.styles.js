import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  // Bilgi metni için stil
  infoText: {
    color: '#666',
    fontSize: 12,
    marginTop: 4,
    marginBottom: 12,
    fontStyle: 'italic',
  },
}); 